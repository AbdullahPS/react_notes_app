import { makeStyles } from "@mui/styles";

export const white = "#FFFFFF";
export const useStyles = makeStyles((theme) => ({
  input: {
    background: white,
    display: "block",
    marginRight: "auto",
    marginLeft: "auto",
  },
}));
